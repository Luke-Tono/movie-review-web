<% if obj.errors.any? %>
  <div class="alert alert-danger">
    <h5 class="alert-heading"><%= pluralize(obj.errors.count, "error") %> occurred:</h5>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>